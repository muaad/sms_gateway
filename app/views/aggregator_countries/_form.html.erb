<%= form_for(@aggregator_country) do |f| %>
  <% if @aggregator_country.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aggregator_country.errors.count, "error") %> prohibited this aggregator_country from being saved:</h2>

      <ul>
      <% @aggregator_country.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aggregator_id %><br>
    <%= f.text_field :aggregator_id %>
  </div>
  <div class="field">
    <%= f.label :country_id %><br>
    <%= f.text_field :country_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
