<%= form_for(@aggregator_ip) do |f| %>
  <% if @aggregator_ip.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@aggregator_ip.errors.count, "error") %> prohibited this aggregator_ip from being saved:</h2>

      <ul>
      <% @aggregator_ip.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :ip_address %><br>
    <%= f.text_field :ip_address %>
  </div>
  <div class="field">
    <%= f.label :aggregator_id %><br>
    <%= f.text_field :aggregator_id %>
  </div>
  <div class="field">
    <%= f.label :whitlisted %><br>
    <%= f.check_box :whitlisted %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
